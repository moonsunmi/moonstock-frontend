{
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "noImplicitAny": false, //              /* 타입 추론이 불가능하고, 타입 선언이 되지 않은 값이 암묵적으로 any 타입으로 사용 허용 여부 */ // true(금지), false(허용)
    "noImplicitOverride": false, //         /* 서브 클래스의 암묵적인 오버라이딩 허용 여부 */                                            // true(금지), false(허용)
    "noImplicitReturns": false, //          /* 함수가 암묵적으로 undefined 반환 허용 여부 */                                            // true(금지), false(허용)
    "strictNullChecks": false,
    "removeComments": true, //              /* 컴파일시 주석 제거 */
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    "typeRoots": ["node_modules/@types", "src/@types"]
  },
  "include": [
    "next-env.d.ts",
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/@types/**/*.d.ts",
    ".next/types/**/*.ts",
    "tailwind.config.js"
  ],
  "exclude": ["node_modules"]
}
