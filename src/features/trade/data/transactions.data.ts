import axiosInstance from '@/lib/axios'

export const transactionsQuery = {}

export const transactionsApi = {
  // async create(body: CreateTradeDto) {
  //   const {data} = await axiosInstance.post('/api/transactions', body, {
  //     headers: {'Content-Type': 'application/json'},
  //     withCredentials: false
  //   })
  //   return data as {trade: ITrade}
  // },
  // async update(id: number, body: UpdateTradeDto) {
  //   const {data} = await axiosInstance.put(`/api/transactions/${id}`, body, {
  //     headers: {'Content-Type': 'application/json'},
  //     withCredentials: false
  //   })
  //   return data as {trade: ITrade}
  // },
  // async remove(id: number) {
  //   const {data} = await axiosInstance.delete(`/api/transactions/${id}`, {
  //     withCredentials: false
  //   })
  //   return data as {ok: true}
  // }
}
